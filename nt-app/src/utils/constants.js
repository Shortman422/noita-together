import { NT } from "@noita-together/nt-message";

// Values for sort options in Room.vue Mods tab
export const SORT_OPTIONS = Object.freeze({
  ALLOWED: NT.ModFlagsListType.ALLOWED,
  DENIED: NT.ModFlagsListType.DENIED,
  USERS: 'users'
});