// Values for modList in Room.vue Mods tab
export const MOD_LIST_TYPES = Object.freeze({
    ALLOWED: 'allowed',
    DENIED: 'denied'
  });

  // Values for sort options in Room.vue Mods tab
  export const SORT_OPTIONS = Object.freeze({
    ALLOWED: MOD_LIST_TYPES.ALLOWED,
    DENIED: MOD_LIST_TYPES.DENIED,
    USERS: 'users'
  });